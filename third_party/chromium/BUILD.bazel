load("@rules_cc//cc:cc_library.bzl", "cc_library")

package(default_visibility = ["//visibility:public"])

cc_library(
    name = "cubic-bezier",
    srcs = ["cubic-bezier.cc"],
    hdrs = ["cubic-bezier.h"],
    tags = ["no-lint"],
    deps = [
        "@abseil-cpp//absl/log:check",
    ],
)
